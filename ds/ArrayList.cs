// auto resizing array
public class ArrayList<T>
{
    // data holder
    private T[] _arrayList;

    public ArrayList()
    {
        // start out with 10
        _arrayList = new T[10];
    }

    // add - no need to return 
    // TODO provide mechanism to grow (iirc if filled halfway then double the size)
    public void Add(T item) {
        // check for size (implement the method first)

        // determine if the size needs adjustment (double check when to double the size)

        // add the item 
    }

    // get (by index)

    // set (change)

    // remove

    // clear

    // size
}